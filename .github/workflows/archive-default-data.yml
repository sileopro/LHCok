# 每年 1 月 1 日 00:00（北京时间）归档开奖 defaultData + 月循 hkwlyxh
name: 年度归档 defaultData / 月循

on:
  schedule:
    # 北京时间 1月1日 00:00 = UTC 12月31日 16:00
    - cron: '0 16 31 12 *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: archive-default-data-${{ github.ref }}
  cancel-in-progress: false

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 设置时区
        uses: szenius/set-timezone@v1.0
        with:
          timezoneLinux: Asia/Shanghai

      - name: 归档 defaultData + 月循 hkwlyxh
        run: python archive_default_data.py
        env:
          TZ: Asia/Shanghai

      - name: 提交归档变更
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add defaultData/ hkwlyxh.txt
          git add -u -- wlhkyxh.txt 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "归档无变更，跳过提交"
            exit 0
          fi
          YEAR=$(date '+%Y')
          PREV=$((YEAR - 1))
          git commit -m "chore(archive): 归档 ${PREV} 年开奖/月循数据 [skip ci]"
          for i in 1 2 3; do
            git fetch origin main
            git rebase origin/main || (git rebase --abort && exit 1)
            git push && exit 0
            echo "Push 失败，重试 $i/3"
            sleep 2
          done
          exit 1
